<div class="container" id="pages-form">
  <div class="row">
    <div class="col-sm-12">
      <h1><%= @news.title %></h1>
      <p><%= @news.created_at %></p>
      <p>
        <%= @news.content.html_safe %>
      </p>

      <%= link_to 'Back', admin_path %>
    </div>
  </div>
</div>
